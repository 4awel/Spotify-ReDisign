// stores/auth.ts

interface ActivityState {

}

export const useActivityStore = defineStore('auth', () => {
  // State
  const state = reactive<ActivityState>({
    
  })

  // Getters
  const userName = computed(() => {});

  // Actions
  const login = async (email: string, password: string) => {

  }

  return {
    // State
    
    // Getters

    
    // Actions

  }
})